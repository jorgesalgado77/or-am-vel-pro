import { supabase } from '@/integrations/supabase/client';

// Re-export the auto-generated client so all files continue to work
export { supabase };

export const EXTERNAL_SUPABASE_URL = "https://bdhfzjuwtkiexyeusnqq.supabase.co";
